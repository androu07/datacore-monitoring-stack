services:
  # --- Contenedor de Prometheus (Almacenamiento de datos) ---
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    restart: unless-stopped
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./alert-manager/rules.yml:/etc/prometheus/rules.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
      - '--storage.tsdb.retention.size=7GB'
      - '--storage.tsdb.wal-compression'
      - '--web.listen-address=192.168.4.210:9090'
    network_mode: "host"

  # --- Contenedor de Grafana (Dashboards) ---
  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    restart: unless-stopped
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_USER:-admin}
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
      - ./grafana/dashboards_json:/var/lib/grafana/dashboards
    ports:
      - "3000:3000"
    depends_on:
      - prometheus

  # --- Contenedor del Gestor de Alarmas ---
  alertmanager:
    image: prom/alertmanager:latest
    container_name: alertmanager
    user: root
    restart: unless-stopped
    volumes:
      - ./alert-manager:/etc/alert-manager
      - alertmanager_data:/alert-manager
    command:
      - '--config.file=/etc/alert-manager/config.yml'
      - '--storage.path=/alert-manager'
      - '--web.listen-address=192.168.4.210:9093'
    network_mode: "host"

  # --- Recolector de Metricas del Sistema Operativo ---
  node-exporter:
    image: prom/node-exporter:latest
    container_name: node-exporter
    restart: unless-stopped
    pid: "host"
    volumes:
      - '/:/host:ro,rslave'
    command:
      - '--path.rootfs=/host'
      - '--web.listen-address=192.168.4.210:9100'
    network_mode: "host"

  # --- Recolector de Datos del Hardware por BMC ---
  ipmi-exporter:
    build:
      context: ./ipmi-exporter
      dockerfile: Dockerfile
    container_name: ipmi-exporter
    restart: unless-stopped
    privileged: true
    devices:
      - /dev/ipmi0:/dev/ipmi0
    command:
      - 'ipmi_exporter'
      - '--config.file=/config/ipmi.yml'
      - '--web.listen-address=192.168.4.210:9290'
    network_mode: "host"

  redfish-exporter:
    build: ./redfish-exporter
    container_name: redfish-exporter
    restart: unless-stopped
    privileged: true
    network_mode: "host"
    environment:
      - LISTEN_ADDR=192.168.4.200
      - LISTEN_PORT=9610

volumes:
  prometheus_data:
  grafana_data:
  alertmanager_data:
